{
  "target_skill": "write-rom-spec",
  "source_path": "skills/testing/write-rom-spec/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when writing specs for ROM Relations and Repositories in Hanami 2.x.",
      "original_snippets": "Use this skill when writing specs for ROM Relations and Repositories in Hanami 2.x.",
      "relevant_when": "When a user asks for Hanami work that should trigger write-rom-spec.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Core principle:** ROM specs test data access logic in isolation. Use in-memory ROM or transactional rollback for speed and isolation.",
      "original_snippets": "**Core principle:** ROM specs test data access logic in isolation. Use in-memory ROM or transactional rollback for speed and isolation.",
      "relevant_when": "When a user asks for Hanami work that should trigger write-rom-spec.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Use transactional rollback** for database isolation:",
      "original_snippets": "**Use transactional rollback** for database isolation:",
      "relevant_when": "When a user asks for Hanami work that should trigger write-rom-spec.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Create test data** using Repositories or direct ROM calls:",
      "original_snippets": "**Create test data** using Repositories or direct ROM calls:",
      "relevant_when": "When a user asks for Hanami work that should trigger write-rom-spec.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Avoid testing ROM itself**. Do not test that `where` works — test your custom query methods.",
      "original_snippets": "**Avoid testing ROM itself**. Do not test that `where` works — test your custom query methods.",
      "relevant_when": "When a user asks for Hanami work that should trigger write-rom-spec.",
      "why_given": "preference"
    }
  ]
}
