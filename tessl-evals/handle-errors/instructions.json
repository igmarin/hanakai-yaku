{
  "target_skill": "handle-errors",
  "source_path": "skills/actions/handle-errors/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when handling errors and halting requests in Hanami 2.x Actions.",
      "original_snippets": "Use this skill when handling errors and halting requests in Hanami 2.x Actions.",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Core principle:** Fail fast and return meaningful error responses. Never swallow exceptions or leak internal details.",
      "original_snippets": "**Core principle:** Fail fast and return meaningful error responses. Never swallow exceptions or leak internal details.",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "preference"
    },
    {
      "instruction": "**Use `halt` for early returns**:",
      "original_snippets": "**Use `halt` for early returns**:",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Halt immediately with a status code and response body for early returns. Do not rely on default success status codes (e.g. 200) for error conditions.",
      "original_snippets": "Halt immediately with a status code and response body for early returns. Do not rely on default success status codes (e.g. 200) for error conditions.",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "preference"
    },
    {
      "instruction": "> **Common Mistake:** Match the error response format to the action format. Do not use `halt` with HTML error pages/strings in a JSON API action.",
      "original_snippets": "> **Common Mistake:** Match the error response format to the action format. Do not use `halt` with HTML error pages/strings in a JSON API action.",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "preference"
    },
    {
      "instruction": "Always rescue `StandardError` (never rescue `Exception` as it catches system-level interrupts like `SIGTERM` or `NoMemoryError`). Log full backtraces internally, but return generic messages externally. Never rescue exceptions silently.",
      "original_snippets": "Always rescue `StandardError` (never rescue `Exception` as it catches system-level interrupts like `SIGTERM` or `NoMemoryError`). Log full backtraces internally, but return generic messages externally. Never rescue exceptions silently.",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "preference"
    },
    {
      "instruction": "Do not manually inspect `request.params.valid?` or duplicate validation inside `#handle`. The action automatically halts with `422 Unprocessable Entity` before `#handle` runs if params validation fails.",
      "original_snippets": "Do not manually inspect `request.params.valid?` or duplicate validation inside `#handle`. The action automatically halts with `422 Unprocessable Entity` before `#handle` runs if params validation fails.",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "preference"
    },
    {
      "instruction": "Always write tests asserting on expected HTTP error status codes (e.g., 404, 422, 500) and verified error payload shapes.",
      "original_snippets": "Always write tests asserting on expected HTTP error status codes (e.g., 404, 422, 500) and verified error payload shapes.",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "reminder"
    },
    {
      "instruction": "Reviewers should check for these red flags:",
      "original_snippets": "Reviewers should check for these red flags:",
      "relevant_when": "When a user asks for Hanami work that should trigger handle-errors.",
      "why_given": "reminder"
    }
  ]
}
