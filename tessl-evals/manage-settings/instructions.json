{
  "target_skill": "manage-settings",
  "source_path": "skills/cross-cutting/manage-settings/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when managing Hanami 2.x application settings.",
      "original_snippets": "Use this skill when managing Hanami 2.x application settings.",
      "relevant_when": "When a user asks for Hanami work that should trigger manage-settings.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Use `.env` files for local development (supported via the `dotenv` gem).",
      "original_snippets": "Use `.env` files for local development (supported via the `dotenv` gem).",
      "relevant_when": "When a user asks for Hanami work that should trigger manage-settings.",
      "why_given": "new knowledge"
    }
  ]
}
