{
  "target_skill": "test-slice",
  "source_path": "skills/slices/test-slice/SKILL.md",
  "instructions": [
    {
      "instruction": "**Goal:** Test a slice's public interface. Never reach into another slice's internals.",
      "original_snippets": "**Goal:** Test a slice's public interface. Never reach into another slice's internals.",
      "relevant_when": "When a user asks for Hanami work that should trigger test-slice.",
      "why_given": "preference"
    },
    {
      "instruction": "**Isolation:** Load only the slice under test. Mock cross-slice dependencies.",
      "original_snippets": "**Isolation:** Load only the slice under test. Mock cross-slice dependencies.",
      "relevant_when": "When a user asks for Hanami work that should trigger test-slice.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Rule:** A slice test must NOT depend on another slice booting.",
      "original_snippets": "**Rule:** A slice test must NOT depend on another slice booting.",
      "relevant_when": "When a user asks for Hanami work that should trigger test-slice.",
      "why_given": "preference"
    },
    {
      "instruction": "Write test → Run test → Verify it FAILS → Implement → Verify it PASSES",
      "original_snippets": "Write test → Run test → Verify it FAILS → Implement → Verify it PASSES",
      "relevant_when": "When a user asks for Hanami work that should trigger test-slice.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "RSpec.describe Api::Actions::Users::Create, :slice do",
      "original_snippets": "RSpec.describe Api::Actions::Users::Create, :slice do",
      "relevant_when": "When a user asks for Hanami work that should trigger test-slice.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Load these files only when needed:",
      "original_snippets": "Load these files only when needed:",
      "relevant_when": "When a user asks for Hanami work that should trigger test-slice.",
      "why_given": "new knowledge"
    }
  ]
}
