{
  "target_skill": "create-validation-contract",
  "source_path": "skills/dry-rb/create-validation-contract/SKILL.md",
  "instructions": [
    {
      "instruction": "Define type-safe, composable validation rules with dry-validation contracts.",
      "original_snippets": "Define type-safe, composable validation rules with dry-validation contracts.",
      "relevant_when": "When a user asks for Hanami work that should trigger create-validation-contract.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Write test → Run test → Verify it FAILS → Implement → Verify it PASSES",
      "original_snippets": "Write test → Run test → Verify it FAILS → Implement → Verify it PASSES",
      "relevant_when": "When a user asks for Hanami work that should trigger create-validation-contract.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Define the contract** — declare expected fields, types, and rules:",
      "original_snippets": "**Define the contract** — declare expected fields, types, and rules:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-validation-contract.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "key.failure(\"must be a valid email\") unless value.match?(URI::MailTo::EMAIL_REGEXP)",
      "original_snippets": "key.failure(\"must be a valid email\") unless value.match?(URI::MailTo::EMAIL_REGEXP)",
      "relevant_when": "When a user asks for Hanami work that should trigger create-validation-contract.",
      "why_given": "preference"
    },
    {
      "instruction": "**Use in the operation** — call the contract as the first step:",
      "original_snippets": "**Use in the operation** — call the contract as the first step:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-validation-contract.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Compose contracts** — inherit or include shared contract modules for common fields (e.g., pagination params).",
      "original_snippets": "**Compose contracts** — inherit or include shared contract modules for common fields (e.g., pagination params).",
      "relevant_when": "When a user asks for Hanami work that should trigger create-validation-contract.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Load these files only when needed:",
      "original_snippets": "Load these files only when needed:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-validation-contract.",
      "why_given": "new knowledge"
    }
  ]
}
