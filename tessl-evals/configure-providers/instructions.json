{
  "target_skill": "configure-providers",
  "source_path": "skills/providers/configure-providers/SKILL.md",
  "instructions": [
    {
      "instruction": "**Define settings** in `config/settings.rb` if the service needs configuration:",
      "original_snippets": "**Define settings** in `config/settings.rb` if the service needs configuration:",
      "relevant_when": "When a user asks for Hanami work that should trigger configure-providers.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Create the provider** at `config/providers/<name>.rb`:",
      "original_snippets": "**Create the provider** at `config/providers/<name>.rb`:",
      "relevant_when": "When a user asks for Hanami work that should trigger configure-providers.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Use `prepare`** for requiring gems and lightweight setup.",
      "original_snippets": "**Use `prepare`** for requiring gems and lightweight setup.",
      "relevant_when": "When a user asks for Hanami work that should trigger configure-providers.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Use `start`** for instantiation and registration. Access other registered components via `target[...]`.",
      "original_snippets": "**Use `start`** for instantiation and registration. Access other registered components via `target[...]`.",
      "relevant_when": "When a user asks for Hanami work that should trigger configure-providers.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Register with a descriptive key** — `\"redis\"`, `\"http_client\"`, not generic names.",
      "original_snippets": "**Register with a descriptive key** — `\"redis\"`, `\"http_client\"`, not generic names.",
      "relevant_when": "When a user asks for Hanami work that should trigger configure-providers.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Verify** — ensure the provider boots without errors. If it needs external resources (database, API), handle connection failures gracefully.",
      "original_snippets": "**Verify** — ensure the provider boots without errors. If it needs external resources (database, API), handle connection failures gracefully.",
      "relevant_when": "When a user asks for Hanami work that should trigger configure-providers.",
      "why_given": "new knowledge"
    }
  ]
}
