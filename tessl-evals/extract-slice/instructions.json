{
  "target_skill": "extract-slice",
  "source_path": "skills/slices/extract-slice/SKILL.md",
  "instructions": [
    {
      "instruction": "**Rule:** Extraction must not change behavior. Run tests before and after.",
      "original_snippets": "**Rule:** Extraction must not change behavior. Run tests before and after.",
      "relevant_when": "When a user asks for Hanami work that should trigger extract-slice.",
      "why_given": "preference"
    },
    {
      "instruction": "DO NOT change behavior during extraction. Every existing test must pass after the move.",
      "original_snippets": "DO NOT change behavior during extraction. Every existing test must pass after the move.",
      "relevant_when": "When a user asks for Hanami work that should trigger extract-slice.",
      "why_given": "preference"
    },
    {
      "instruction": "**Create the target slice** — use `create-slice` to scaffold the new slice structure.",
      "original_snippets": "**Create the target slice** — use `create-slice` to scaffold the new slice structure.",
      "relevant_when": "When a user asks for Hanami work that should trigger extract-slice.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Update imports** — any remaining code referencing the old namespace must be updated. Check:",
      "original_snippets": "**Update imports** — any remaining code referencing the old namespace must be updated. Check:",
      "relevant_when": "When a user asks for Hanami work that should trigger extract-slice.",
      "why_given": "preference"
    },
    {
      "instruction": "**Verify** — run the full test suite. Every test that passed before must pass after:",
      "original_snippets": "**Verify** — run the full test suite. Every test that passed before must pass after:",
      "relevant_when": "When a user asks for Hanami work that should trigger extract-slice.",
      "why_given": "preference"
    },
    {
      "instruction": "Load these files only when needed:",
      "original_snippets": "Load these files only when needed:",
      "relevant_when": "When a user asks for Hanami work that should trigger extract-slice.",
      "why_given": "new knowledge"
    }
  ]
}
