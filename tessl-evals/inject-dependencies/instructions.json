{
  "target_skill": "inject-dependencies",
  "source_path": "skills/di/inject-dependencies/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when injecting dependencies into Hanami 2.x components.",
      "original_snippets": "Use this skill when injecting dependencies into Hanami 2.x components.",
      "relevant_when": "When a user asks for Hanami work that should trigger inject-dependencies.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Core principle:** Dependencies are injected, not looked up. Never access the container directly — always use `include Deps[]`.",
      "original_snippets": "**Core principle:** Dependencies are injected, not looked up. Never access the container directly — always use `include Deps[]`.",
      "relevant_when": "When a user asks for Hanami work that should trigger inject-dependencies.",
      "why_given": "preference"
    },
    {
      "instruction": "**Always use `include Deps[]`** to inject dependencies:",
      "original_snippets": "**Always use `include Deps[]`** to inject dependencies:",
      "relevant_when": "When a user asks for Hanami work that should trigger inject-dependencies.",
      "why_given": "reminder"
    },
    {
      "instruction": "These are handled by ROM and should not be auto-registered by dry-system.",
      "original_snippets": "These are handled by ROM and should not be auto-registered by dry-system.",
      "relevant_when": "When a user asks for Hanami work that should trigger inject-dependencies.",
      "why_given": "reminder"
    },
    {
      "instruction": "**Inject multiple dependencies** at once:",
      "original_snippets": "**Inject multiple dependencies** at once:",
      "relevant_when": "When a user asks for Hanami work that should trigger inject-dependencies.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Never access the container directly**:",
      "original_snippets": "**Never access the container directly**:",
      "relevant_when": "When a user asks for Hanami work that should trigger inject-dependencies.",
      "why_given": "preference"
    }
  ]
}
