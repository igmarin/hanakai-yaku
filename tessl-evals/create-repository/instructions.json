{
  "target_skill": "create-repository",
  "source_path": "skills/db/create-repository/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when creating ROM Repositories that encapsulate domain-level persistence logic in Hanami 2.x.",
      "original_snippets": "Use this skill when creating ROM Repositories that encapsulate domain-level persistence logic in Hanami 2.x.",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Create the Repository file**:",
      "original_snippets": "**Create the Repository file**:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Verify container registration**:",
      "original_snippets": "**Verify container registration**:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "After creating the repository, confirm it is correctly registered in the Hanami container before wiring it into actions. Run in the console:",
      "original_snippets": "After creating the repository, confirm it is correctly registered in the Hanami container before wiring it into actions. Run in the console:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Inject into Actions**:",
      "original_snippets": "**Inject into Actions**:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Inject repositories using the container dependency injection (`Deps`):",
      "original_snippets": "Inject repositories using the container dependency injection (`Deps`):",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Write specific read and write methods to isolate your actions from raw relation access:",
      "original_snippets": "Write specific read and write methods to isolate your actions from raw relation access:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Use transactions for multi-step writes**:",
      "original_snippets": "**Use transactions for multi-step writes**:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Do not expose Relations directly**:",
      "original_snippets": "**Do not expose Relations directly**:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "preference"
    },
    {
      "instruction": "Actions must fetch and modify data via Repositories. Bypassing repositories to query relations directly in actions/views is an anti-pattern.",
      "original_snippets": "Actions must fetch and modify data via Repositories. Bypassing repositories to query relations directly in actions/views is an anti-pattern.",
      "relevant_when": "When a user asks for Hanami work that should trigger create-repository.",
      "why_given": "preference"
    }
  ]
}
