{
  "target_skill": "manage-database",
  "source_path": "skills/cli/manage-database/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when running Hanami 2.x database CLI commands.",
      "original_snippets": "Use this skill when running Hanami 2.x database CLI commands.",
      "relevant_when": "When a user asks for Hanami work that should trigger manage-database.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Core principle:** Database commands are environment-aware. Always confirm `HANAMI_ENV` and `DATABASE_URL` before running destructive commands.",
      "original_snippets": "**Core principle:** Database commands are environment-aware. Always confirm `HANAMI_ENV` and `DATABASE_URL` before running destructive commands.",
      "relevant_when": "When a user asks for Hanami work that should trigger manage-database.",
      "why_given": "reminder"
    },
    {
      "instruction": "**Create the database:**",
      "original_snippets": "**Create the database:**",
      "relevant_when": "When a user asks for Hanami work that should trigger manage-database.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Run migrations** (validate with `hanami db version` - prints timestamp of last applied migration):",
      "original_snippets": "**Run migrations** (validate with `hanami db version` - prints timestamp of last applied migration):",
      "relevant_when": "When a user asks for Hanami work that should trigger manage-database.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Run seed data** (`db/seeds.rb` is executed):",
      "original_snippets": "**Run seed data** (`db/seeds.rb` is executed):",
      "relevant_when": "When a user asks for Hanami work that should trigger manage-database.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "> **`hanami db drop` deletes all data and cannot be undone. Always validate before proceeding.**",
      "original_snippets": "> **`hanami db drop` deletes all data and cannot be undone. Always validate before proceeding.**",
      "relevant_when": "When a user asks for Hanami work that should trigger manage-database.",
      "why_given": "reminder"
    }
  ]
}
