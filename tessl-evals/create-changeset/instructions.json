{
  "target_skill": "create-changeset",
  "source_path": "skills/db/create-changeset/SKILL.md",
  "instructions": [
    {
      "instruction": "Define write rules for a relation. Every data mutation goes through a changeset — never call `relation.insert` directly.",
      "original_snippets": "Define write rules for a relation. Every data mutation goes through a changeset — never call `relation.insert` directly.",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Types:** `ROM::Changeset::Create`, `ROM::Changeset::Update`, `ROM::Changeset::Delete`.",
      "original_snippets": "**Types:** `ROM::Changeset::Create`, `ROM::Changeset::Update`, `ROM::Changeset::Delete`.",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Write test → Run test → Verify it FAILS → Implement → Verify it PASSES",
      "original_snippets": "Write test → Run test → Verify it FAILS → Implement → Verify it PASSES",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "DO NOT call relation.command(:create).call(...) directly. Use a changeset.",
      "original_snippets": "DO NOT call relation.command(:create).call(...) directly. Use a changeset.",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Create the changeset** for the target relation:",
      "original_snippets": "**Create the changeset** for the target relation:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "class CreateUser < ROM::Changeset::Create",
      "original_snippets": "class CreateUser < ROM::Changeset::Create",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Use in the repository** — the repository's `create` method delegates to the changeset:",
      "original_snippets": "**Use in the repository** — the repository's `create` method delegates to the changeset:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Compose changesets** — chain multiple changesets with `.data` for multi-step writes.",
      "original_snippets": "**Compose changesets** — chain multiple changesets with `.data` for multi-step writes.",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Load these files only when needed:",
      "original_snippets": "Load these files only when needed:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-changeset.",
      "why_given": "new knowledge"
    }
  ]
}
