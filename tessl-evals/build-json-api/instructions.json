{
  "target_skill": "build-json-api",
  "source_path": "skills/actions/build-json-api/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when building JSON API endpoints in Hanami 2.x Actions.",
      "original_snippets": "Use this skill when building JSON API endpoints in Hanami 2.x Actions.",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Core principle:** JSON APIs must produce predictable, parseable output. Implement round-trip parse → serialize → parse verification for all serializers.",
      "original_snippets": "**Core principle:** JSON APIs must produce predictable, parseable output. Implement round-trip parse → serialize → parse verification for all serializers.",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "preference"
    },
    {
      "instruction": "**Use a dedicated serializer object** for complex APIs:",
      "original_snippets": "**Use a dedicated serializer object** for complex APIs:",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Verify round-trip parse → serialize → parse** for every serializer:",
      "original_snippets": "**Verify round-trip parse → serialize → parse** for every serializer:",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    }
  ]
}
