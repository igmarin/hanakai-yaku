{
  "target_skill": "build-json-api",
  "source_path": "skills/actions/build-json-api/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when building JSON API endpoints in Hanami 2.x Actions.",
      "original_snippets": "Use this skill when building JSON API endpoints in Hanami 2.x Actions.",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Create the action** and set `response.format = :json`",
      "original_snippets": "**Create the action** and set `response.format = :json`",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Write a round-trip test** asserting serialize → parse produces correct fields",
      "original_snippets": "**Write a round-trip test** asserting serialize → parse produces correct fields",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Verify**: run the test; fix serializer if fields are missing or misformatted",
      "original_snippets": "**Verify**: run the test; fix serializer if fields are missing or misformatted",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Use dedicated serializers** to encode response bodies.",
      "original_snippets": "**Use dedicated serializers** to encode response bodies.",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Verify round-trip serialization**:",
      "original_snippets": "**Verify round-trip serialization**:",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Always assert that a serialized object can be parsed back into equivalent data. Format all date/time fields with `.iso8601`. If the assertion fails, check that the serializer maps all fields and that timestamps use `.iso8601`.",
      "original_snippets": "Always assert that a serialized object can be parsed back into equivalent data. Format all date/time fields with `.iso8601`. If the assertion fails, check that the serializer maps all fields and that timestamps use `.iso8601`.",
      "relevant_when": "When a user asks for Hanami work that should trigger build-json-api.",
      "why_given": "reminder"
    }
  ]
}
