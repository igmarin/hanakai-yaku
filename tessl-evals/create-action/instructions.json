{
  "target_skill": "create-action",
  "source_path": "skills/actions/create-action/SKILL.md",
  "instructions": [
    {
      "instruction": "Use this skill when creating or reviewing Hanami 2.x Actions.",
      "original_snippets": "Use this skill when creating or reviewing Hanami 2.x Actions.",
      "relevant_when": "When a user asks for Hanami work that should trigger create-action.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Generate** the Action file. One Action per file — generate `Index`, `Show`, `Create`, `Update`, `Destroy` as separate classes:",
      "original_snippets": "**Generate** the Action file. One Action per file — generate `Index`, `Show`, `Create`, `Update`, `Destroy` as separate classes:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-action.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Define a Params block** to validate and coerce input. Invalid params should `halt 422` before any business logic runs:",
      "original_snippets": "**Define a Params block** to validate and coerce input. Invalid params should `halt 422` before any business logic runs:",
      "relevant_when": "When a user asks for Hanami work that should trigger create-action.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "class Create < MyApp::Action",
      "original_snippets": "class Create < MyApp::Action",
      "relevant_when": "When a user asks for Hanami work that should trigger create-action.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "**Inject dependencies** via `Deps` — never access the container directly (`Hanami.app['key']` is untestable and forbidden):",
      "original_snippets": "**Inject dependencies** via `Deps` — never access the container directly (`Hanami.app['key']` is untestable and forbidden):",
      "relevant_when": "When a user asks for Hanami work that should trigger create-action.",
      "why_given": "new knowledge"
    }
  ]
}
